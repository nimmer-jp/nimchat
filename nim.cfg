# Basolato を v0.15 系に固定（Nim 2.2.8 + 新系 Basolato での GC-safety エラー回避）。
# `nimble install` 後に pkgcache が生成される。
--path:"$home/.nimble/pkgcache/githubcom_itsumurahnimbasolato_#v0.15.0/src"
--path:"."
--nimcache:"./nimcache"
